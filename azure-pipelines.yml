# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger: none

pool: Self-Agent

steps: 
- task: TerraformTask@5
  inputs:
    provider: 'azurerm'
    command: 'init'
    workingDirectory: '$(System.DefaultWorkingDirectory)/GiRepo1'
    backendAzureRmResourceGroupName: 'JK-RG'
    backendAzureRmStorageAccountName: 'stgyamlpipe'
    backendAzureRmContainerName: 'yamlconteiner'
    backendAzureRmKey: 'MY'